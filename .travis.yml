sudo: false

language:
  - python

python:
  - 2.7
  - 3.6

install:
  - pip install -r requirements.txt

script:
  - nosetests --with-coverage --cover-package=pystream --cover-min-percentage=100

notifications:
  email: false
